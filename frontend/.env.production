# Base URL for the AetherGuard API.
# Leave empty when FastAPI serves this build (same-origin).
# Set to the Render service URL for a split deploy, e.g.
# VITE_API_BASE_URL=https://aetherguard-api.onrender.com
VITE_API_BASE_URL=
