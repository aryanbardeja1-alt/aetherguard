numpy>=1.24
scipy>=1.11
astropy>=6.0
# Pinned: newer poliastro releases do not build on Python 3.12+, so pip
# backtracks to 0.7.0. Verified working against astropy 8.x on Python 3.14.
poliastro==0.7.0
pytest>=8.0
